[project]
name = "test-greeter"
version = "0.1.0"
description = "A simple greeting tool using random templates"
requires-python = ">=3.10"

[project.scripts]
greet = "greet:main"

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
